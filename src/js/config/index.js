(function(angular){
    'use strict';
    const _requires = [
        'coreConfig',
        'themeConfig',
        'routeConfig',
        'httpConfig',
    ];
    angular.module('appConfig', _requires);
})(window.angular);